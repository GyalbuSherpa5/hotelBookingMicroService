server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotelbook_inventory_service
    username: root
    password: Basketball

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
    show-sql: true

  application:
    name: inventory-service

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

project:
  image: inventoryService/src/main/resources/static/